import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'newproject622022',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
